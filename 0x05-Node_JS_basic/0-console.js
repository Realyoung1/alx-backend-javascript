/**
 * displayMessage: displaying messages in the console
 * @message {message to be printed to stdout}
 */

const displayMessage = (message) => {
  console.log(message);
};

module.exports = displayMessage;
